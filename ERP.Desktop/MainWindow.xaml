<Window x:Class="ERP.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ERP.Desktop.Converters"
        xmlns:behaviors="clr-namespace:ERP.Desktop.Behaviors"
        Title="Nina ERP"
        Height="420"
        Width="520"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
        <Window.Resources>
                <BooleanToVisibilityConverter x:Key="BoolToVis"/>
                <local:InverseBooleanConverter x:Key="InverseBool"/>

                <!-- Animação Spinner para Loading -->
                <Storyboard x:Key="SpinAnimation"
                                RepeatBehavior="Forever">
                        <DoubleAnimation
                                Storyboard.TargetName="SpinnerRectangle"
                                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                From="0"
                                        To="360"
                                        Duration="0:0:2"/>
                </Storyboard>
        </Window.Resources>

        <Grid Margin="20">
                <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Cabeçalho -->
                <TextBlock Text="ERP Nina"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#333"/>
                <TextBlock Grid.Row="1"
                           Text="Sistema Integrado de Gestão Empresarial"
                           Margin="0,5,0,20"
                           Foreground="#666"
                           FontSize="12"/>

                <!-- Formulário -->
                <StackPanel Grid.Row="2"
                            Orientation="Vertical">
                        <!-- Usuário -->
                        <TextBlock Text="Usuário"
                                   FontWeight="SemiBold"
                                   Foreground="#333"/>
                        <TextBox x:Name="UsernameBox"
                                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                                 Width="400"
                                 Height="36"
                                 Padding="10,8"
                                 Margin="0,5,0,15"
                                 IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBool}}"/>

                        <!-- Senha -->
                        <TextBlock Text="Senha"
                                   FontWeight="SemiBold"
                                   Foreground="#333"
                                   Margin="0,0,0,5"/>
                        <PasswordBox x:Name="PasswordBox"
                                     Width="400"
                                     Height="36"
                                     Padding="10,8"
                                     Margin="0,0,0,15"
                                     behaviors:PasswordBoxBehavior.IsLoading="{Binding IsLoading}"/>
                </StackPanel>

                <!-- Botão -->
                <Button Grid.Row="3"
                        Content="Entrar"
                        Width="120"
                        Height="40"
                        HorizontalAlignment="Left"
                        Margin="0,10,0,0"
                        Click="OnLoginClicked"
                        IsEnabled="{Binding IsFormValid}"
                        Background="#0078D4"
                        Foreground="White"
                        FontWeight="SemiBold"
                        Cursor="Hand"/>

                <!-- Mensagem de Status -->
                <TextBlock Grid.Row="4"
                           Text="{Binding StatusMessage}"
                           Foreground="#D32F2F"
                           Margin="0,20,0,0"
                           TextWrapping="Wrap"
                           FontSize="12"
                           Visibility="{Binding StatusMessage, Converter={StaticResource BoolToVis}}"/>

                <!-- Indicador de Loading com Animação -->
                <StackPanel Grid.Row="4"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="0,20,0,0"
                            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                        <Rectangle x:Name="SpinnerRectangle"
                                   Width="16"
                                   Height="16"
                                   Margin="0,0,10,0"
                                   Fill="#0078D4"
                                   RenderTransformOrigin="0.5,0.5">
                                <Rectangle.RenderTransform>
                                        <RotateTransform Angle="0"/>
                                </Rectangle.RenderTransform>
                                <Rectangle.Triggers>
                                        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                                <BeginStoryboard Storyboard="{StaticResource SpinAnimation}"/>
                                        </EventTrigger>
                                </Rectangle.Triggers>
                        </Rectangle>
                        <TextBlock Text="Conectando ao servidor..."
                                   Foreground="#0078D4"
                                   FontSize="12"
                                   VerticalAlignment="Center"/>
                </StackPanel>
        </Grid>
</Window>
